import React from "react";
import Logout from "./Logout"; // Import logout component

const Navbar = () => {
    return (
        <nav>
            <h2>My App</h2>
            <Logout />
        </nav>
    );
};

export default Navbar;
